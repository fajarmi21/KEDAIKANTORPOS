<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.polinema.android.kotlin.kedaikantorpos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".admin.pembayaranadmin"></activity>
        <activity android:name=".inputdatamenu" />
        <activity android:name=".MainActivity" />
        <activity android:name=".admin.AdminActivity" />
        <activity android:name=".admin.detail_transaksi_admin" />
        <activity android:name=".detail_transaksi" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".KeranjangActivity" />
        <activity android:name=".TransaksiActivity" />
        <activity android:name=".PemesananActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".Pertanyaan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>